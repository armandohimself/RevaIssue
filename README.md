# RevaIssue
> Spring Boot application for managing users and projects with role-based access.

## Team Project Planning 
We used Spring Initializr and selected: 
* Spring Web
* Spring Data JPA (if using DB)
* Lombok
* Gradle 9.2.1

Driver (SQLite is a bit custom; often H2/Postgres are smoother)

#### Build Tool
We decided that our build tool will be Gradle and you'll see our build.gradle file here. Why?

 

### Agreed Standards & Practices
1. Our file folder structure follows `src/main/java/com/abra/revaissue/`. 
2. Our **Main.js** file is RevaIssueApplication.java
3. 


## Run the application

Navigate to abra-studio/RevaIssue and run the following command to start the application:

```bash
./gradlew bootRun
```
Navigate to `http://localhost:8081`.  

