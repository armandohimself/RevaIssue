# RevaIssue
> Spring Boot application for managing users and projects with role-based access.




## Run the application

Navigate to abra-studio/RevaIssue and run the following command to start the application:

```bash
./gradlew bootRun
```
Navigate to `http://localhost:8081`.  

